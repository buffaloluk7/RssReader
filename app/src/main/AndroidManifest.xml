<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="cc.lukas.rssreader" >

    <uses-permission android:name="android.permission.INTERNET" />
    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".activities.MainActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <service
            android:name=".services.RssFeedDownloadService"
            android:exported="false" >
        </service>

        <provider
            android:name="cc.lukas.rssreader.RssFeedContentProvider"
            android:authorities="cc.lukas.rssreader.provider.rssfeed"
            android:exported="false" />
        <provider
            android:name="cc.lukas.rssreader.RssItemContentProvider"
            android:authorities="cc.lukas.rssreader.provider.rssitem"
            android:exported="false" />
    </application>
</manifest>
